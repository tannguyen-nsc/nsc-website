<?php
/*
Template Name: NSC Career job detail (static build)
 *
 * Renders the static frontend build career-details.html.
 * For real job openings use the Job CPT: URLs use single-job.php + templates/single-job.twig
 * (NSC Theme Options → Careers for sidebar + Contact Form 7 apply shortcode).
 * Optional WP page slug "career-details" can still point here for static preview / pageMap.
 */

$buildTemplate = 'career-details.html';
require __DIR__ . '/template-static-build-page.php';
